{{!-- <section id="sar-attackOption-panel" class="attackOption-panel" style="{{topStyle}}">
</section> --}}


{{!-- <div class="my-custom-panel">
    <h2>{{actor.name}}</h2>
    <p>Tipo: {{actor.type}}</p>
    <p>Habilidades:</p>
    <ul>
        {{#each actor.data.abilities}}
            <li>{{name}}: {{value}}</li>
        {{/each}}
    </ul>
</div> --}}

<div id="action-remaining" class="action-remaining-panel"></div>
  {{!-- {{#each squares}}
  <div class="square-item">
    <span class="label">{{name}}:</span>
    <div class="square-row">
      {{#times quantity}}
      <div class="square"></div>
      {{/times}}
    </div>
  </div>
  {{/each}} --}}
</div>